apiVersion: v1
kind: Service
metadata:
  name: studypartner-service
  namespace: studypartner
spec:
  type: NodePort
  selector:
    app: studypartner
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
